{
  "name": "nuxt-app",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "dev:debug": "npx wrangler pages dev --kv=IMAGES --live-reload -- npm run dev",
    "dev:run": "npx wrangler pages dev dist --kv=IMAGES",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "@nuxt/devtools": "latest",
    "@nuxthq/ui": "^2.7.0",
    "@sidebase/nuxt-auth": "^0.6.0-beta.3",
    "@types/node": "^18.16.19",
    "nuxt": "^3.6.5",
    "wrangler": "^3.4.0"
  },
  "dependencies": {
    "@cloudflare/workers-types": "^4.20230724.0",
    "@upstash/redis": "^1.22.0"
  }
}
